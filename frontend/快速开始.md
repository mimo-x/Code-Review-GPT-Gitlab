# 🚀 快速开始指南

## 前置准备

确保你的系统已安装:
- Node.js >= 18.0 ([下载地址](https://nodejs.org/))
- npm 或 pnpm 或 yarn

## 一键启动

### 1. 进入前端目录
```bash
cd frontend
```

### 2. 安装依赖
```bash
npm install
```

如果安装慢，可以使用国内镜像:
```bash
npm install --registry=https://registry.npmmirror.com
```

### 3. 启动开发服务器
```bash
npm run dev
```

看到以下输出表示启动成功:
```
VITE v5.0.0  ready in 500 ms

➜  Local:   http://localhost:3000/
➜  Network: use --host to expose
```

### 4. 打开浏览器
访问 http://localhost:3000

你将看到:
- 🎨 深色侧边栏布局
- 📊 仪表盘页面(默认页)
- 📊 数据统计卡片
- 📈 图表展示

## 页面导航

点击侧边栏菜单,访问不同页面:

| 菜单 | 路径 | 功能 |
|-----|------|-----|
| 仪表盘 | `/dashboard` | 数据统计与图表 |
| 审查记录 | `/reviews` | 审查历史列表 |
| 配置管理 | `/config` | LLM/GitLab配置 |
| 日志监控 | `/logs` | 系统日志查看 |
| 项目信息 | `/about` | 系统信息展示 |

## 移动端访问

1. 确保手机和电脑在同一局域网
2. 启动时添加 `--host` 参数:
```bash
npm run dev -- --host
```

3. 访问显示的 Network 地址(如 `http://192.168.1.100:3000`)

## 生产构建

```bash
# 构建
npm run build

# 预览构建结果
npm run preview
```

构建产物在 `dist/` 目录。

## 常见问题

### Q: 端口被占用
A: 修改 `vite.config.ts` 中的端口号:
```typescript
server: {
  port: 3001,  // 改为其他端口
  ...
}
```

### Q: 依赖安装失败
A:
1. 删除 `node_modules` 和 `package-lock.json`
2. 清理npm缓存: `npm cache clean --force`
3. 重新安装: `npm install`

### Q: 如何连接后端API
A: 修改 `vite.config.ts` 中的代理配置:
```typescript
proxy: {
  '/api': {
    target: 'http://your-backend-url:80',
    changeOrigin: true
  }
}
```

## 技术支持

遇到问题? 查看:
- 📖 [README.md](./README.md) - 完整文档
- 📝 [项目说明.md](./项目说明.md) - 项目详情
- 🌐 [Preline文档](https://preline.co/docs/)
- 🎨 [Tailwind文档](https://tailwindcss.com/docs)

## 下一步

1. ✅ 熟悉各个页面功能
2. ✅ 查看代码结构(src/views/*.vue)
3. ✅ 对接后端API(src/api/index.ts)
4. ✅ 自定义样式(src/styles/main.css)
5. ✅ 添加新功能

祝你开发愉快! 🎉
